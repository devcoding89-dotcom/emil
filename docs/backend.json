
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's public profile information.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "isAdmin": {
          "type": "boolean",
          "description": "Whether the user has administrative privileges."
        }
      },
      "required": [
        "displayName",
        "email"
      ]
    },
    "Extraction": {
      "title": "Extraction Result",
      "description": "Stores the result of an email extraction process.",
      "type": "object",
      "properties": {
        "rawText": {
          "type": "string",
          "description": "The original raw text that was parsed."
        },
        "emails": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "email"
          },
          "description": "The list of unique emails extracted from the text."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the extraction was performed."
        }
      },
      "required": [
        "rawText",
        "emails",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "email-password",
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores public user profile data."
    },
    "/users/{userId}/extractions/{extractionId}": {
      "schema": {
        "$ref": "#/entities/Extraction"
      },
      "description": "Stores a user's history of email extraction results."
    }
  }
}
